{% extends 'base.html.twig' %}

{% block title %}Gestión de Libros{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2>📚 Libros</h2>
            <a href="{{ path('admin_libro_nuevo') }}" class="btn btn-success">
                ➕ Nuevo libro
            </a>
    </div>

    <table class="table table-striped table-bordered align-middle">
        <thead class="table-dark">
            <tr>
                <th>Título</th>
                <th>Autor</th>
                <th>Género</th>
                <th>Estado</th>
                <th>Imagen</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for libro in libros %}
            <tr>
                <td>{{ libro.titulo }}</td>
                <td>{{ libro.autor }}</td>
                <td>{{ libro.genero }}</td>
                <td>
                    {% if libro.estado.value == 'Disponible' %}
                        <span class="badge bg-success">Disponible</span>
                    {% elseif libro.estado.value == 'Prestado' %}
                        <span class="badge bg-warning text-dark">Prestado</span>
                    {% else %}
                        <span class="badge bg-secondary">{{ libro.estado.value }}</span>
                    {% endif %}
                </td>
                <td>
                    {% if libro.imagen %}
                        <img src="{{ asset('uploads/libros/' ~ libro.imagen) }}" 
                             alt="Imagen de {{ libro.titulo }}" 
                             width="60" class="img-thumbnail">
                    {% else %}
                        <span class="text-muted">Sin imagen</span>
                    {% endif %}
                </td>
                <td class="text-center">
                    <a href="{{ path('admin_libro_editar', {id: libro.id}) }}" class="btn btn-sm btn-info">Editar</a>
                    <a href="{{ path('admin_libro_eliminar', {id: libro.id}) }}" 
                       class="btn btn-sm btn-danger"
                       onclick="return confirm('¿Seguro que quieres eliminar este libro?');">
                        Eliminar
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6" class="text-center text-muted">No hay libros cargados.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
