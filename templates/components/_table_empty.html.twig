
{# templates/components/_table_empty.html.twig #}
<tr>
    <td colspan="{{ colspan|default(5) }}" class="text-center text-muted">
        {{ message|default('No hay registros disponibles.') }}
    </td>
</tr>
